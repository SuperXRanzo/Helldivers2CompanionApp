<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
        tools:targetApi="31">

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.admin.AdminDashboardActivity" />
        <activity android:name=".ui.player.PlayerDashboardActivity" />
        <activity android:name=".MainActivity" />

        <activity android:name=".ui.admin.ManageStratagemsActivity" />
        <activity android:name=".ui.admin.WarStatusActivity" />
        <activity android:name=".ui.admin.ManageMissionsActivity" />
        <activity android:name=".ui.admin.AnalyticsActivity" />

        <activity android:name=".ui.player.StratagemListActivity" />
        <activity android:name=".ui.player.ProfileActivity" />
        <activity android:name=".ui.player.PlayerWarStatusActivity" />
        <activity android:name=".ui.player.MissionListActivity" />
        <activity android:name=".ui.player.SquadActivity" />
        <activity android:name=".ui.player.PracticeStratagemActivity" />
        <activity android:name=".ui.player.LoadoutActivity" />

    </application>

</manifest>